[build]
  command = "hugo --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.160.1"
  HUGO_ENV = "production"

[context.production]
  [context.production.environment]
    HUGO_ENV = "production"